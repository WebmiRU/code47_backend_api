apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: repman-tls
  namespace: default
spec:
  secretName: repman-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - repman.b220.ru
